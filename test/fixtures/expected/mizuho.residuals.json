[
  {
    "category": "rider_eligibility",
    "class": "b",
    "statement": "The feed does not carry rider eligibility conditions — who may use the service, and under what circumstances. GTFS-Flex has no field for them and MSD v0.1.0 has no key for them, so nothing is asserted about eligibility either way.",
    "model_coverage": "documented_candidate",
    "candidate": "eligibility"
  },
  {
    "category": "membership_requirement",
    "class": "b",
    "statement": "The feed does not state whether membership or registration is a precondition of booking, nor any lead time such a registration would need. Nothing in the document implies that booking is open to all.",
    "model_coverage": "documented_candidate",
    "candidate": "membership_requirement"
  },
  {
    "category": "per_channel_booking_metadata",
    "class": "b",
    "statement": "The feed states which booking channels exist, but not their hours, credentials or capabilities. Those are described in the free-text booking message, which is preserved verbatim in diagnostics and never parsed into fields.",
    "model_coverage": "documented_candidate",
    "candidate": "booking-channel object shape"
  },
  {
    "category": "fares",
    "class": "b",
    "statement": "MSD can express fares: fare_structures is defined in the schema. This feed carries no fare information — no fare_attributes.txt, fare_rules.txt or fare product files — so the key is absent rather than empty.",
    "model_coverage": "field_exists",
    "field": "fare_structures"
  },
  {
    "category": "payment_methods",
    "class": "b",
    "statement": "MSD can express accepted payment methods, within a fare structure. This feed states none, and carries no fare structure to hold them, so the key is absent.",
    "model_coverage": "field_exists",
    "field": "payment_methods"
  },
  {
    "category": "organisational_parties",
    "class": "b",
    "statement": "MSD can express the legal entity behind a provider, with its registration and VAT identifiers. This feed identifies the operating agency only, so provider.legal_entity is absent.",
    "model_coverage": "field_exists",
    "field": "legal_entity"
  },
  {
    "category": "vehicles",
    "class": "b",
    "statement": "MSD can express the fleet: vehicle types, seat and wheelchair capacities, propulsion and counts. This feed describes none of it, so the key is absent — and no accessibility assertion is derived, because an absent wheelchair_boarding in GTFS is not the same statement as MSD false.",
    "model_coverage": "field_exists",
    "field": "vehicles"
  },
  {
    "category": "web_booking_channel",
    "class": "b",
    "statement": "MSD can express a web booking channel: \"web\" is a value of the booking_channel code list. This feed leaves booking_url empty, so no web channel is asserted — the channel list is built from what is structurally present and never inferred from the booking message, which may describe a portal the feed does not name.",
    "model_coverage": "field_exists",
    "field": "booking_channels",
    "evidence": {
      "source_field": "booking_rules.booking_url",
      "value": ""
    }
  },
  {
    "category": "provider_telephone",
    "class": "a",
    "statement": "The feed carries a provider telephone number. MSD v0.1.0 provider has no telephone property — only contact_email — so the number is kept in diagnostics rather than written to the document.",
    "model_coverage": "undocumented_gap",
    "coverage_note": "An observation about the coverage of MSD v0.1.0 — not a defect, not a request and not a proposal.",
    "evidence": {
      "source_field": "agency.agency_phone"
    }
  },
  {
    "category": "service_description",
    "class": "a",
    "statement": "The feed carries a description for each route. MSD v0.1.0 service has no description property, although provider does, so the text is kept in diagnostics.",
    "model_coverage": "undocumented_gap",
    "coverage_note": "An observation about the coverage of MSD v0.1.0 — not a defect, not a request and not a proposal.",
    "evidence": {
      "source_field": "routes.route_desc",
      "routes": [
        "mizuhomachi_route"
      ]
    }
  },
  {
    "category": "stop_metadata",
    "class": "a",
    "statement": "The feed carries stop columns beyond identity, name and position. An MSD stop holds only stop_id, name and coordinates, so the remaining columns are kept in diagnostics.",
    "model_coverage": "undocumented_gap",
    "coverage_note": "An observation about the coverage of MSD v0.1.0 — not a defect, not a request and not a proposal.",
    "evidence": {
      "columns": [
        "location_type"
      ]
    }
  },
  {
    "category": "location_group_name",
    "class": "a",
    "statement": "The location group carries a name. MSD service_area has no name property, so the group name is kept in diagnostics; the group itself becomes the stops-based service area.",
    "model_coverage": "undocumented_gap",
    "coverage_note": "An observation about the coverage of MSD v0.1.0 — not a defect, not a request and not a proposal.",
    "evidence": {
      "source_field": "location_groups.location_group_name"
    }
  },
  {
    "category": "booking_phone_number",
    "class": "a",
    "statement": "The booking rule carries a telephone number. MSD records that a phone channel exists but has no field for the number itself, so it is kept in diagnostics.",
    "model_coverage": "documented_candidate",
    "candidate": "booking-channel object shape",
    "evidence": {
      "source_field": "booking_rules.phone_number"
    }
  },
  {
    "category": "booking_instructions_free_text",
    "class": "a",
    "statement": "The booking rule carries free-text instructions for riders. MSD has no field for them and this bridge does not parse prose into structured fields, so the text is preserved verbatim in diagnostics.",
    "model_coverage": "undocumented_gap",
    "coverage_note": "An observation about the coverage of MSD v0.1.0 — not a defect, not a request and not a proposal.",
    "evidence": {
      "source_field": "booking_rules.message"
    }
  },
  {
    "category": "service_calendar_range",
    "class": "c",
    "statement": "Each calendar states the date range over which it applies. MSD operating_hours models weekly patterns and dated exceptions but carries no validity range for the service, so a reader of the document cannot see when the pattern began or when it is expected to end.",
    "model_coverage": "documented_candidate",
    "candidate": "service-level valid_from / valid_until",
    "evidence": {
      "source_fields": [
        "calendar.start_date",
        "calendar.end_date"
      ],
      "ranges": [
        "20241001–20260930"
      ]
    }
  },
  {
    "category": "feed_validity_end",
    "class": "c",
    "statement": "The feed states a date until which the feed itself is valid. That is a statement about the document rather than about the service, and MSD has no equivalent field, so a consumer cannot see when the publisher expects this description to be superseded.",
    "model_coverage": "undocumented_gap",
    "coverage_note": "An observation about the coverage of MSD v0.1.0 — not a defect, not a request and not a proposal.",
    "evidence": {
      "source_field": "feed_info.feed_end_date",
      "value": "20260930"
    }
  },
  {
    "category": "translations",
    "class": "c",
    "statement": "The feed carries translated labels for stop, route and agency names. They are secondary labels rather than names, and MSD v0.1.0 has no field for alternates, so they are kept in diagnostics and never substituted into any name in the document. The absence of an alternate-name field is a recorded observation upstream and explicitly not a candidate for a future version.",
    "model_coverage": "undocumented_gap",
    "coverage_note": "An observation about the coverage of MSD v0.1.0 — not a defect, not a request and not a proposal.",
    "evidence": {
      "rows": 126,
      "languages": [
        "en"
      ]
    }
  }
]
