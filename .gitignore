# Node dependencies
node_modules/

# Build artefacts
dist/
*.tgz

# Logs and OS cruft
*.log
.DS_Store

# The term list now lives in .git/hooks/terms.txt, read by the local pre-commit
# guard — inside .git/, so never tracked. This entry stays as a backstop against
# a stray copy being restored into the repository root out of habit.
compliance-terms.txt

# Local Claude Code config / guardrails — not for the public repo
.claude/
CLAUDE.md

# Roundtrip artefacts — regenerate with `npm run roundtrip`
build/

# Canonical GTFS validator JAR — fetched by pinned version, never committed
*.jar
