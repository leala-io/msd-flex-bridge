# Node dependencies
node_modules/

# Build artefacts
dist/
*.tgz

# Logs and OS cruft
*.log
.DS_Store

# Compliance term list — the clear-text source of scripts/compliance-blocklist.json.
# Deliberately untracked: publishing a readable list of forbidden terms would be the
# disclosure the gate exists to prevent. Regenerate with `npm run blocklist`.
compliance-terms.txt

# Local Claude Code config / guardrails — not for the public repo
.claude/
CLAUDE.md
